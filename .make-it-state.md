# Project State — Buildwright
> Last updated: 2026-06-10
> Last session: make-it (initial build)

## Current Status
App builds clean (zero errors/warnings), all 20 unit tests pass, .app bundle assembles and launches, tmux machinery live-verified against tmux 3.6b. Ready for first real use.

## Build Completed
- Phase 0: Preflight — PASSED (git, Docker n/a, gh, Swift 6.3 toolchain, tmux 3.6b, node, claude)
- Phase 1: Ideation — COMPLETE
- Phase 2: Design — COMPLETE (project_type: desktop-app, Tier 0 guardrails; web tiers documented as skipped in .make-it/app-context.json)
- Phase 3: Build — COMPLETE
- Phase 4: Ship — PENDING (local tool; ship = push to GitHub if desired)

## What Was Built
- Native macOS app (SwiftUI + SwiftTerm + AppKit), SPM package, macOS 14+
- tmux topology: workspace = session (default socket, clean names for iPad attach), pane = window, hidden `_bw-*` grouped sessions for per-pane display
- Workspaces → tabs → split-pane layout tree (rows/columns/nested, drag-resize), JSON persistence + reconcile-on-launch
- Claude status pipeline: hooks (UserPromptSubmit/PreToolUse → working, Notification → needs-input, Stop → done) → bw-hook → status files → app badges + macOS notifications + tmux status bar
- Backlog sidebar: full parser/writer for ~/.claude/backlog (frontmatter order preserved, BOARD.md regenerated), per-workspace filters, search, detail/edit sheets, create epic/story, Start → Claude pane with `/backlog start <id>`
- Browser panes (WKWebView, dock left/right), CVR launch sheet (runs record.ts in a shell pane, AppleScript window docking with graceful degradation)
- Remote Access settings panel (SSH/mosh/tmux/Tailscale checks + copy-paste Blink commands), bw companion CLI
- Scripts/bundle.sh → dist/Buildwright.app (ad-hoc signed); `--install` copies to /Applications

## Build-Verify Results
- swift build: PASSED (0 errors, 0 warnings)
- swift test: PASSED (20/20, Swift Testing via Xcode toolchain — set DEVELOPER_DIR)
- Live tmux verification: session/window creation with env vars PASSED; grouped session + select-window PASSED; attach with client PASSED; bw-hook status files PASSED; bw ls PASSED; status-bar set PASSED
- Found + fixed live: tmux 3.6 rejects "=name" exact-match prefix for set-option targets → switched to "name:" form
- App launch: PASSED (process stable, installs ~/.local/bin/bw + bw-hook, merges 4 hooks into ~/.claude/settings.json non-destructively)

## Known Issues
- Tests require the full Xcode toolchain (CLT lacks XCTest/Testing): use `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test`
- AppleScript `quit` event not handled (use ⌘Q); kill is safe — tmux owns all processes
- CVR Chromium auto-docking needs Accessibility permission (one-time); degrades gracefully
- mosh not yet installed on this Mac (Remote Access panel guides `brew install mosh`)

## Next Steps
- Run the app: `open dist/Buildwright.app` (or ./Scripts/bundle.sh --install)
- Create the docai workspace and try Start on a backlog story
- Optional: push to GitHub (`gh repo create`), install mosh, set up Tailscale on iPad
