# Project State — Buildwright
> Last updated: 2026-06-11
> Last session: wrap-it
> Session ended: 2026-06-11

## Current Status
v0.7.0 installed at /Applications/Buildwright.app and in daily-use shape. Builds clean,
55/55 tests pass, all work committed and pushed to github.com/sealmindset/buildwright.
The document-ai tmux workspace keeps running independently of the app (by design).
NOTE: quit + relaunch the app to pick up v0.7.0 if the running instance is older.

## This Session (2026-06-11, resume-it)
- v0.4.0: browser tabs — Chrome-style tab strip per browser pane with + button; new tabs open blank with the address bar focused; tab labels follow page titles
- target=_blank / window.open / ⌘-clicked links now open as new tabs (were silently inert)
- All tabs + active tab persist across restarts; pre-tab state files migrate automatically (normalizeBrowserTabs)
- WebViewCache now keyed by tab (was pane); 11 new BrowserTabTests cover the tab model
- v0.5.0: per-project bookmarks — ☆ star in address bar saves page (editable popover: name/URL/shared toggle); bookmarks bar under address bar; per-workspace sets + shared-across-workspaces group
- Bookmark management via right-click (open in new tab / rename / move workspace↔shared / delete); ⌘-click opens new tab; ⌘K palette lists all bookmarks and opens a browser pane if none exists
- 5 new BookmarkTests (codable round-trips + old-state-file compatibility)
- v0.6.0: trackpad scrolling in tmux panes — local NSEvent monitor routes scrolls over a TmuxTerminalView to tmux as SGR wheel events (SwiftTerm scrollWheel is public-not-open, so monitor instead of override); delta accumulation (3 lines/event vs tmux's 5-lines-per-wheel) keeps swipe ≈ content distance; 5 new WheelScrollTests
- v0.7.0: private browsing by default — browser panes use a shared nonPersistent WKWebsiteDataStore (one per app run; in-session logins work across tabs, all evaporates on quit); per-pane glasses toggle in tab strip flips private↔persistent (webviews recreated, tabs reload); Settings → General → Browser default toggle (browserPrivateByDefault, on); legacy panes (nil flag) follow the app default; 4 new BrowserPrivacyTests

## Previous Session (2026-06-11)
- v0.3.1: Claude panes now launch with --dangerously-skip-permissions by default; toggle in Settings → General (applies to new panes); persisted across restarts
- Fixed bundle.sh codesign failure (strip Finder metadata/xattrs before signing) so --install always works first try
- Reinstalled to /Applications and verified running; all changes pushed to GitHub

## Previous Session (2026-06-10)
- Initial build (v0.1.0): full native macOS terminal IDE — tmux-backed panes, workspaces/tabs/splits, Claude status via hooks, backlog sidebar with Start, browser panes, CVR launcher, Remote Access panel, bw CLI
- v0.1.1: fixed "can't find session: _bw-…" (tmux reaps detached destroy-unattached sessions instantly; replaced with explicit cleanup at launch/quit) + attach-before-session race
- v0.2.0: attention queue + ⌘J, menu-bar waiting count, status ages, "while you were away" re-entry strips, Breakfix/Feature templates (editable in Settings → Templates)
- v0.3.0: ⌘K command palette, ⌥⌘-arrow spatial pane navigation, ⌘⌥1-9 workspace hotkeys, backlog close-the-loop ("mark item done" button)
- Installed to /Applications; private GitHub repo created and synced

## Outstanding Items
See TODO.md — all deferred-by-choice ideas (layout presets, browser profiles, focus mode, app icon) plus two setup steps (mosh install, Accessibility permission for CVR docking).

## Test Status
- Unit tests: 55 passing, 0 failing (Swift Testing; requires DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer)
- Integration: tmux machinery live-verified against tmux 3.6b
- E2E: manual (native GUI app)
- Last run: 2026-06-11 (v0.7.0 build)

## Known Issues
- AppleScript `quit` event not handled (⌘Q works; force-kill is safe — tmux owns all processes)
- Tests need full Xcode toolchain (CLT lacks the Testing module)

## Suggested Next Steps
1. Quit + relaunch Buildwright to pick up v0.7.0 — then try tabs, bookmarks, trackpad scrolling, and private browsing; report scroll feel (3-lines-per-event constant is tunable)
2. Live in it for a few days — let real use reveal which deferred ideas actually matter
3. `brew install mosh` + Settings → Remote Access to wire up the iPad/Blink workflow
3. First time using CVR docking, approve Buildwright in System Settings → Privacy & Security → Accessibility
