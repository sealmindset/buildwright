# Project State — Buildwright
> Last updated: 2026-06-18
> Last session: resume-it

## Current Status
Native macOS terminal IDE (Swift/SwiftUI + SwiftTerm, tmux-backed). `main` is clean and
pushed (origin/main = `e7fbbba`). This session built out the **AI Scrum Master** across the
`backlog` skill + the Buildwright cockpit: **E50** intake (capture) and **E51** code-grounded
reconciliation + autonomous dispatch, plus a standing ship preamble with green-gate + harm-gate.
Prior line (E46/E48) added the resilience safety net — output coalescing, per-pane flow control,
resource governor, circuit-breaker, hang watchdog, agent reaping — plus the plugin mechanism with
CVR as the first plugin. A fresh release build (still versioned 0.34.0/build 37 — script hardcodes
it, not bumped) is installed at /Applications/Buildwright.app; **quit + relaunch to pick up the
new GUI (⌘⇧N capture + Reconcile panel)**. The skill subcommands work in any Claude Code session
without the app.

## This Session (2026-06-18, resume-it)
- **E51 — code-grounded reconciliation + autonomous dispatch** (two subagents, disjoint repos):
  - Skill: `/backlog reconcile [<id>|all]` runs a code→tests→live verification ladder against
    docai (`~/Documents/GitHub/docai`), returns per-item verdicts (built/partial/not-built/unknown
    + confidence + evidence), auto-applies high-confidence reversible board mutations (provenance +
    undo) and flags the rest. `/backlog dispatch` starts the next parallel-safe gap under the ship
    preamble. EPIC-51 holds the canonical contract. Live + pushed; skill backup refreshed.
  - App: `Reconciler` engine + Reconcile cockpit panel (verdicts/evidence, auto-applied vs flagged
    + accept/reject + undo, dispatch arm toggle). docai path is a setting (generalizes to other
    repos). Merged `e7fbbba`, pushed, **73/73 tests pass**, built + installed.
  - Verification ladder: BUILT only if code ✓ AND tests ✓ AND live ✓ — never a silent false "done".
    Auto-apply threshold 0.8 + reversible + non-harmful; else flag (harm-gate). Dispatch is armed by
    default per rvance's choice, one-at-a-time, parallel-safe, reversible.
- **Standing ship preamble** added to the `backlog` skill (auto-prepended to every `/backlog start`,
  covering the app's Start button too): "Launch subagents" + clarify-first + build → **100%-green
  hard gate** → ship. Two tracks: safe/reversible (incl. Buildwright) ships fully autonomously;
  potentially-harmful/app-impacting (docai/splashdown, destructive, auth/security/infra, breaking,
  externally-visible) stops at an open PR for human approval ("when in doubt, gate").
- Backed up the `backlog` skill into the repo (`skills/backlog/SKILL.md` + restore note); committed
  E49 (Template Maker breakfix); backlog repo is clean/synced.
- **E50 — AI Scrum Master intake** earlier this session via two subagents (disjoint repos, parallel):
  - Skill door: extended the `backlog` schema (`size: XS|S|M|L|XL`; new `breakfix`/`spike` types,
    backward-compatible), added `/backlog capture` (auto-file triage, no approval gate) and
    `/backlog undo`, plus BOARD rendering (`[S]` size chip, `⚡` breakfix, `🔬` spike). Canonical
    contract lives in EPIC-50's design.md. Committed + pushed to sealmindset/backlog.
  - App door: SwiftUI quick-capture sheet on `⌘⇧N` → headless `claude -p` triage (same JSON
    contract as the skill) → auto-file into the board via BacklogStore → `✓ Filed … — Undo` toast.
    `BacklogItem` gained `size`; wired into AppState/BuildwrightApp/CommandPalette like
    ScrumMaster/Groomer. (`⌘⇧N` freed from "New Claude Pane".)
  - Merged to main (`--no-ff`, 2615efb), pushed origin, built release + installed.
- Triage design: best existing-epic fit < 0.6 → AI mints a new epic; otherwise stacks. Every
  auto-filed card carries a provenance block (raw text verbatim + reasoning + confidence) so
  auto-file is auditable and reversible.
- **Live smoke test** of the skill door: captured the long-standing selection-wash bug → filed as
  **E46-S18** (breakfix, S) under Buildwright Resilience (confidence 0.62), pushed, Slack-notified.
  The selection-wash bug is now a tracked board item (was only in TODO.md).

## Previous Session (2026-06-16 → 17, resume-it → wrap-it)
- Investigated the "full-pane selection wash on epic finish" bug. No code changed — diagnosis only.
- Traced it into SwiftTerm's selection model. High-confidence mechanism: the wash is a real
  text selection (inverse video), not the exited-overlay capsule. SwiftTerm only clears
  selections on streaming output when mouse reporting is ON (`feedPrepare`/`linefeed` guard
  on `allowMouseReporting`). When an agent process exits, its TUI turns mouse reporting OFF,
  so SwiftTerm preserves the selection and it renders over the whole pane. `keyDown`,
  `mouseDown` (single click), `resizeSubviews`, and the ~4s idle-converge refresh each reset
  `selection.active` — which is why it clears when you "work on something else."
- OPEN question that gates the fix: what *creates* the whole-buffer selection at completion.
  Decides between "clear selection on the exited transition" vs. fixing a gesture path.
  Deliberately not fixing on theory (see [[debug-with-live-tests-not-theory]]).

## Outstanding Items
See TODO.md. Top of mind: relaunch the app, then GUI smoke-test both `⌘⇧N` capture and the
Reconcile panel; **first reconcile against docai should be reconcile-only / dry-run** before
arming dispatch at the live product. EPIC-50 and EPIC-51 are in-progress (skill + app done —
remaining: real-world tuning of triage/reconcile quality from the GUI). Selection-wash bug is
tracked as E46-S18 (still needs the creator confirmed before fixing); plus E46 Phase 2/3.

## Test Status
- Unit tests: **73 passing** (Swift Testing; +4 E50 capture, +14 E51 reconcile/dispatch over the ~55 baseline).
  Requires DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer.
- Integration: tmux machinery live-verified previously; skill capture verified live (E46-S18).
  Reconcile/dispatch not yet run live against docai.
- E2E: manual (native GUI app) — `⌘⇧N` capture + Reconcile panel not yet exercised in the running app.
- Last run: 2026-06-18 (release build + `swift test` on merged main `e7fbbba`, all 73 green).

## Known Issues
- Full-pane "selection wash" when an epic finishes — diagnosed, not yet fixed. Now tracked as
  board item E46-S18 (was TODO.md only).
- AppleScript `quit` event not handled (⌘Q works; force-kill safe — tmux owns processes).
- Tests need the full Xcode toolchain (CLT lacks the Testing module).
- Bundle version string hardcoded at 0.34.0/build 37 in Scripts/bundle.sh — not bumped for E50.

## Suggested Next Steps
0. Quit + relaunch /Applications/Buildwright.app to load E50/E51 GUI (⌘⇧N + Reconcile panel).
   Then do a **reconcile-only / dry-run** against docai first; review verdicts before arming dispatch.
1. Smoke-test `⌘⇧N`: capture a throwaway thought → confirm triage/file/`✓ Filed … — Undo` toast →
   hit Undo (or `/backlog undo`).
2. Confirm the selection-wash creator (E46-S18): (a) does ⌘C copy the whole screen when washed?
   (b) shell panes too or only agent panes? — or an instrumented build logging selection state
   at pane-exit. Then fix with a hard guarantee and verify on a live build.
3. E46 Phase 2 — `buildwrightd` supervisor daemon (backlog E46-S7…S11).
